import cart from './assets/cart.png'
const CartWidget = () => {
return (
        <div>
            <img src={cart} alt="Carrito de compras" />
            0
        </div>
    )
}

export default CartWidget;